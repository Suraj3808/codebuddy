// level {1/2/3}


##level
2.

const arr1 = [1, 2, 3];
arr1.reduce((accum, total) => accum + total)

3.

for(items of objects) { retuen items; }


#level2

1.

const arr1 = [1, 3, 4, 6, 7, 8];

const arr2 = arr1.map((value) => {
    if (value % 2 === 0) {
        return true;
    } else {
        retuen false;
    }
})
